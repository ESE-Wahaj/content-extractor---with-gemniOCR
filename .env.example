# Example environment variables for local development
# Copy this file to `.env.local` (or `.env`) and add your real API key.
# NOTE: For Create React App env vars must begin with REACT_APP_ and you must restart the dev server after editing.

REACT_APP_GEMINI_API_KEY="your_gemini_api_key_here"
